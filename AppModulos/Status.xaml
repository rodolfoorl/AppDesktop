<UserControl x:Class="Desktop.GrupoBW.AppModulos.Status"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Desktop.GrupoBW.AppModulos"
             xmlns:theme="http://metro.mahapps.com/winfx/xaml/controls">
    <Grid>
        <Border Grid.Row="1" BorderBrush="{StaticResource MahApps.Brushes.Gray7}" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Row="1" BorderBrush="{StaticResource MahApps.Brushes.Gray7}" BorderThickness="0,0,1,0">
                    <TextBlock x:Name="StatusVersao" HorizontalAlignment="Center" Foreground="Gray" FontSize="10" Margin="5"/>
                </Border>

                <theme:MetroProgressBar x:Name="StatusProgressBar" IsIndeterminate="True" Visibility="Collapsed" Grid.Column="1"></theme:MetroProgressBar>
            </Grid>
        </Border>
    </Grid>
</UserControl>
